<%= form_with(model: videoplaylist) do |form| %>
  <% if videoplaylist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(videoplaylist.errors.count, "error") %> prohibited this videoplaylist from being saved:</h2>

      <ul>
        <% videoplaylist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
